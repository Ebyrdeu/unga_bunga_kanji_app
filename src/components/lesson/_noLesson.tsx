import {Text} from '@mantine/core';

export const NoLesson = () => {
  return (
      <Text align={'center'} fz="xl" tt="uppercase" fw={700} c="dimmed">
        No lesson available
      </Text>
  );
};
